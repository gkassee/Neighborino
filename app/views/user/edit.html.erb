<body id='user-edit-body'>

<div id="user-edit-wrapper">
	<% if current_user.address1 == nil %>
		
		<p class="edit-head">Awesome!</p>
		<p class="edit-head">Now, let's get you setup.</p>
		<p id="edit-instructions">Below, let us know where you live and what we should call you. Don't worry, you can update these details at any time.</p>

	<% else %>
		
		<h1>Need to make a change?</h1>

	<% end %>

	<%= form_for @user, :id => "edit-form", url: {action: 'update'} do |f| %>

		<ul id="edit-list">

			<li>
			<%= f.text_field :name, :class => "input-box", :placeholder => "*USERNAME" %>
			</li>

			<li>
			<%= f.text_field :address1, :class => "input-box", :placeholder => "*STREET" %>
			</li>

			<li>
			<%= f.text_field :city, :class => "input-box", :placeholder => "*CITY" %>
			</li>

			<li>
			<%= f.text_field :state, :class => "input-box", :placeholder => "*STATE/PROVINCE" %>
			</li>

			<li>
			<%= f.text_field :zip, :class => "input-box", :placeholder => "*ZIP CODE" %>
			</li>

		</ul>
			<% if current_user.address1 == nil %>

				<%= f.submit "CREATE ACCOUNT", :class => "edit-submitter" %>

			<% else %>
			
				<%= f.submit "SAVE", :class => "edit-submitter" %>

		<% end %>

	<% end %>
</div>

</body>
